package designpatterns.structural.facade;

/**
 * Used when we have to hide system complexity from the client
 */

/**
 * Example 1: We have a Dao and want to only expose certain methods like insert() and update() in contrast to 100s of other methods
 *
 * It is different from proxy in the sense that It can take care of multiple classes
 */
public class Main {
}
